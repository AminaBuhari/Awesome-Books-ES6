const books = window.document.getElementById('books-container');
const formTitle = window.document.getElementById('title');
const formAuthor = window.document.getElementById('author');
const form = window.document.getElementById('form');
const navItems = window.document.querySelectorAll('.nav-item');
const sectionBooks = window.document.querySelectorAll('.sect');

export { books };
export { formTitle, formAuthor, form };
export { navItems, sectionBooks };
